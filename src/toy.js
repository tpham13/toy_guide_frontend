//console log to make sure toy.js is loaded or can be read
console.log("in toy js");
//to get the list of toy, we need to send in the toy params from the back-end in here
class Toy {
    //  constructor( ) {
        this.id = data.id
        // this.title
        // this.description
        // this.price
        // this.url
        // this.toy_category_id
    }

}